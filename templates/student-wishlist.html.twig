{% extends 'base.html.twig' %}

{% block title %}Ma wishlist - Help Me Stage{% endblock %}

{% block stylesheets %}
    <link rel="stylesheet" href="/assets/css/student-dashboard.css">
{% endblock %}

{% block body %}
<main class="student-page">
    <div class="student-container">
        <section class="student-section">
            <div class="student-section-header">
                <h2>Ma wishlist</h2>
                <a href="/espace-etudiant">Retour au dashboard</a>
            </div>

            <div class="student-list">
                {% for offer in wishlistOffers %}
                    <article class="student-row">
                        <div class="student-main">
                            <h3>{{ offer.titre }}</h3>
                            <p>{{ offer.entreprise_nom }} • {{ offer.lieu ?: 'Lieu non défini' }}</p>
                            <small>{{ offer.duree_semaines }} semaines • {{ offer.remuneration }} €</small>
                        </div>

                        <div class="wishlist-actions">
                            <a href="/offres/{{ offer.id }}" class="student-link">Voir l'offre</a>

                            <form method="POST" action="/offres/{{ offer.id }}/wishlist/supprimer">
                                <input type="hidden" name="_csrf_token" value="{{ csrf_token }}">
                                <button type="submit" class="wishlist-remove" aria-label="Retirer de la wishlist">✕</button>
                            </form>
                        </div>
                    </article>
                {% else %}
                    <p class="empty-state">Aucune offre dans votre wishlist.</p>
                {% endfor %}
            </div>
        </section>
    </div>
</main>
{% endblock %}